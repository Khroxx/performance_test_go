services:
  gobackend:
    build: .
    environment:
      DB_HOST: host.docker.internal
      DB_PORT: 5432
      DB_USER: testuser
      DB_PASSWORD: testpassword
      DB_NAME: testdb
    extra_hosts:
      - "host.docker.internal:host-gateway"
    ports:
      - "8081:8081"

